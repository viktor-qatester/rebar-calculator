services:
  - type: web
    name: rebar-calculator-bot
    runtime: python
    plan: free
    buildCommand: pip install --upgrade pip && pip install -r requirements-bot.txt
    startCommand: python run_webhook.py
    healthCheckPath: /health
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false
